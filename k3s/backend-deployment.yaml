# backend-deployment.yaml
# Tells Kubernetes how to run the FastAPI backend.
# Keeps 2 replicas running at all times for high availability.
# Uses RollingUpdate so there's zero downtime during deployments.
#
# Apply: kubectl apply -f k3s/backend-deployment.yaml -n emergencyq

apiVersion: apps/v1
kind: Deployment
metadata:
  name: emergencyq-backend
  namespace: emergencyq
  labels:
    app: emergencyq-backend

spec:
  replicas: 1  # 2 copies: one handles traffic if the other crashes

  selector:
    matchLabels:
      app: emergencyq-backend

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1        # Spin up 1 extra pod during updates
      maxUnavailable: 0  # Never take a pod down before the new one is ready (zero downtime)

  template:
    metadata:
      labels:
        app: emergencyq-backend
    spec:
      containers:
        - name: backend
          image: knightsmokers/emergencyq-backend:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 8000

          # Load env vars from ConfigMap (non-sensitive) and Secret (sensitive)
          envFrom:
            - configMapRef:
                name: emergencyq-config
            - secretRef:
                name: emergencyq-secrets

          # Resource limits — ML model needs ~800MB RAM at startup
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"

          # Startup: Give the model plenty of time to load from swap,
          # checking frequently so it becomes ready as soon as possible.
          startupProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 30  # Give it 30 seconds before starting checks
            periodSeconds: 15        # Check every 15 seconds
            failureThreshold: 40     # 40 * 15s = 600s (10 minutes) maximum startup time

          # Liveness: Is the pod alive? Restart it if this fails.
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
            failureThreshold: 3

          # Readiness: Is the pod ready to receive traffic?
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
